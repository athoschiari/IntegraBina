program Exemplo2L;

uses
  Forms,
  Unit2L in 'Unit2L.pas' {Form1},
  UnitCH in 'UnitCH.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
